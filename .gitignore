# Secrets & Config
.env
.env.local
.env.*.local
.env.backup
.env.production

# Scripts avec secrets inline (deploy / build / lancement local)
upcyclevps
upcyclemacos
upcyclelocal

# Binaire compilé par build.sh (embarque le payload, ne doit JAMAIS être pushé)
uc
uc.x.c

# Rapport PDF — local uniquement, ne doit pas être poussé
UpcycleConnect-Rapport.pdf

# Front
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/*.log

# Back
backend/server
backend/*.exe
backend/tmp/

# Uploads
uploads/

# Bdd
upcycleconnect.sql
database/init/*.sql

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
.fleet/
*.swp
*.swo

# Docker
docker-compose.override.yml

# Exports/dumps de prod (contiennent secrets + données) — JAMAIS sur GitHub
export/
deploy-lab.sh
apache/certs/
